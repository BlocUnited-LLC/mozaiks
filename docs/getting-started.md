# Getting Started

Install Mozaiks and open the Console.

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install mozaiks
mozaiks quickstart --dir .\my-first-mozaiks-app
```

Then open:

```text
http://localhost:3000/apps
```

Click `Create App` to start the build workflow.

## Next

Use [Local Setup](local-setup.md) only if you are developing Mozaiks itself.
