"""Backend package for the factory_control_plane identity module.

The declarative module system mounts this zero-action module contract for
Studio identity. The control-plane harness runtime is owned by
`mozaiksai/control_plane/`, and the first-party factory pack lives under
`factory_app/control_plane/`.
"""
