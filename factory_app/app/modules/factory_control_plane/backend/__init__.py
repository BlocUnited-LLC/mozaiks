"""Backend package for the factory_control_plane identity module.

The declarative module system mounts this zero-action module contract for
Studio identity. The refinement engine runtime currently lives in
`mozaiksai/control_plane/` for import compatibility, and the first-party
factory harness lives under `factory_app/refinement_harness/`.
"""
