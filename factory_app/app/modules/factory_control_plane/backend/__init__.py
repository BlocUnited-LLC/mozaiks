"""Backend package for the factory_control_plane module stub.

The declarative/dynamic module system can still mount this module contract, but
the real control-plane harness runtime is owned by `mozaiksai/control_plane/`
and the first-party factory pack lives under `factory_app/control_plane/`.
"""
