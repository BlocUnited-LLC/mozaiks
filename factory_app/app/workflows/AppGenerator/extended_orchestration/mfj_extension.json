{
  "version": 3,
  "mid_flight_journeys": [
    {
      "id": "app-task-generation",
      "description": "AppPlanAgent decomposes the app build into explicit task runs, fans out parallel AppGenerator children, then AssemblyAgent merges their code files before validation.",
      "decomposition_agent": "AppPlanAgent",
      "fan_out": {
        "spawn_mode": "workflow",
        "max_children": 12
      },
      "fan_in": {
        "resume_agent": "AssemblyAgent",
        "inject_as": "mfj_app_task_results"
      }
    }
  ]
}
