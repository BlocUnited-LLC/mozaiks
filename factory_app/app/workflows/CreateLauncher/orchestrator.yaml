workflow_name: CreateLauncher
max_turns: 12
human_in_the_loop: true
workflow_startup_mode: AgentDriven
orchestration_pattern: DefaultPattern
initial_message_to_user: null
initial_message: >
  You are the Mozaiks create launcher. CreateLaunchAgent, greet the user,
  determine whether they are starting a new app or working from an existing
  app, then use the workflow-local launcher tool to enter the shared create
  journey.
initial_agent: CreateLaunchAgent