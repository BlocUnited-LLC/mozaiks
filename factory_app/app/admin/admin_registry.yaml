schema_version: mozaiks.admin.registry.v1

# First-party Studio routes are declared in app/ui/route_manifest.json.
# This registry is intentionally reserved for AdminPortal extension pages used
# by module/admin panels. Do not duplicate Studio product routes here.

pages: []
