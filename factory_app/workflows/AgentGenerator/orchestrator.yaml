schema_version: mozaiks.orchestrator.v1
workflow_name: AgentGenerator
max_turns: 25
human_in_the_loop: true
workflow_startup_mode: AgentDriven
orchestration_pattern: ag2_network
initial_message: InterviewAgent, check the approved app context for actual AI
  requirements. Ordinary deterministic modules and pages do not require an AI
  workflow. If no AI work is requested, emit NEXT immediately. Otherwise ask
  only a missing question that changes the required AI workflow design.
initial_agent: InterviewAgent

