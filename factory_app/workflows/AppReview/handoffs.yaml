handoff_rules:
- source_agent: ReviewAgent
  target_agent: user
  handoff_type: after_work
  condition_type: null
  condition_scope: null
  condition: null
  transition_target: RevertToUserTarget
- source_agent: ReviewAgent
  target_agent: terminate
  handoff_type: condition
  condition_type: expression
  condition_scope: null
  condition: ${review_complete} == True
  transition_target: TerminateTarget
- source_agent: user
  target_agent: ReviewAgent
  handoff_type: condition
  condition_type: string_llm
  condition_scope: null
  condition: When the user wants to make changes or request revisions to the generated app.
  transition_target: AgentTarget
