definitions:
  approval_outcome:
    type: string
    authority_class: closed_writer_routing_state
    writer_ids: [deterministic_tool]
    persisted: true
    source: {type: state, default: error}
  approval_attempts:
    type: integer
    authority_class: closed_writer_quality_state
    writer_ids: [deterministic_tool]
    persisted: true
    source: {type: state, default: 0}
  artifact_outcome:
    type: string
    authority_class: closed_writer_routing_state
    writer_ids: [deterministic_tool]
    persisted: true
    source: {type: state, default: error}
  artifact_attempts:
    type: integer
    authority_class: closed_writer_quality_state
    writer_ids: [deterministic_tool]
    persisted: true
    source: {type: state, default: 0}
agents: {}
