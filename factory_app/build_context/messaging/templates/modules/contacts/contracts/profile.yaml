schema_version: mozaiks.profile.v1
panels:
  - id: contact_list
    title: Contacts
    description: People you have added as contacts.
    order: 35
    kind: list
    action: list_contacts
    fields:
      - id: contact_user_id
        label: User
        type: string
      - id: created_at
        label: Added
        type: date
