# Backend HTTP base URL (no trailing slash)
MOZAIKS_API_URL=http://localhost:8000

# Backend WebSocket base URL — defaults to API_URL with http replaced by ws.
# Only set this if your WS endpoint is on a different host or port.
# MOZAIKS_WS_URL=ws://localhost:8000
