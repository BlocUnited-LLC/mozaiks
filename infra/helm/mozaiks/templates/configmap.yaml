apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mozaiks.fullname" . }}-config
  labels:
    {{- include "mozaiks.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
