# mozaiksai Requirements
# Core runtime
ag2[a2a,openai,tracing]==1.0.2
a2a-sdk[grpc]>=1.1.0,<2
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
websockets>=11.0
pydantic>=2.5.0
python-dotenv>=1.0.0
pyyaml>=6.0
httpx
aiohttp>=3.8.0
tree-sitter>=0.20.0
tree-sitter-python
tree-sitter-javascript
tree-sitter-css

# Database
motor>=3.3.0
pymongo>=4.5.0
dnspython>=2.4.0

# Auth
PyJWT[crypto]>=2.8.0
python-multipart>=0.0.6

# Azure (optional — used when AZURE_KEYVAULT_URL is set)
azure-identity>=1.15.0
azure-keyvault-secrets>=4.7.0
# Azure Blob Storage (optional — used when MOZAIKS_MEDIA_CONTENT_BACKEND=azure_blob)
azure-storage-blob>=12.30.1

