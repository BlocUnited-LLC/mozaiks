{
  "name": "@mozaiks/chat-ui",
  "private": true,
  "version": "1.0.0",
  "description": "Mozaiks Chat UI library. This package.json exists so that dependencies used by src/ are resolvable via standard Node module resolution when developing from template/.",
  "type": "module",
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18",
    "react-router-dom": ">=6"
  },
  "dependencies": {
    "dompurify": "^3.1.6",
    "marked": "^13.0.3",
    "react-icons": "^5.3.0"
  }
}
