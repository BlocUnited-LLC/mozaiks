# Context placeholder mode (for local testing without seeded DB records)
#
# Usage:
#   1) Copy this value into your local `.env`
#   2) Keep the JSON file path pointing to a placeholder payload
#   3) Start runtime and run workflow as usual

MOZAIKS_CONTEXT_PLACEHOLDERS_FILE=platform/config/context_placeholders.example.json
