handoff_rules:
- source_agent: user
  target_agent: WritersHostAgent
  handoff_type: condition
  condition_type: expression
  condition_scope: pre
  condition: ${is_child_workflow} != True and ${room_seed_ready} != True
  transition_target: AgentTarget

- source_agent: WritersHostAgent
  target_agent: DecompositionAgent
  handoff_type: condition
  condition_type: expression
  condition_scope: after
  condition: ${is_child_workflow} != True and ${room_seed_ready} == True
  transition_target: AgentTarget

- source_agent: WritersHostAgent
  target_agent: user
  handoff_type: after_work
  condition_type: null
  condition_scope: null
  condition: null
  transition_target: RevertToUserTarget

- source_agent: ResumeRouterAgent
  target_agent: HostAgent
  handoff_type: condition
  condition_type: expression
  condition_scope: pre
  condition: ${_mfj_resume_pending} == True and ${_mfj_resume_target_agent} == "HostAgent" and ${_mfj_resume_nonce} != ${_mfj_resume_consumed_nonce}
  transition_target: AgentTarget

- source_agent: ResumeRouterAgent
  target_agent: user
  handoff_type: after_work
  condition_type: null
  condition_scope: null
  condition: null
  transition_target: RevertToUserTarget

- source_agent: HostAgent
  target_agent: terminate
  handoff_type: condition
  condition_type: expression
  condition_scope: after
  condition: ${writers_summary} != null
  transition_target: TerminateTarget

- source_agent: HostAgent
  target_agent: user
  handoff_type: after_work
  condition_type: null
  condition_scope: null
  condition: null
  transition_target: RevertToUserTarget

- source_agent: RoastLaneAgent
  target_agent: terminate
  handoff_type: condition
  condition_type: expression
  condition_scope: after
  condition: ${is_child_workflow} == True
  transition_target: TerminateTarget

- source_agent: ObservationalLaneAgent
  target_agent: terminate
  handoff_type: condition
  condition_type: expression
  condition_scope: after
  condition: ${is_child_workflow} == True
  transition_target: TerminateTarget

- source_agent: AbsurdistLaneAgent
  target_agent: terminate
  handoff_type: condition
  condition_type: expression
  condition_scope: after
  condition: ${is_child_workflow} == True
  transition_target: TerminateTarget

- source_agent: CrowdWorkLaneAgent
  target_agent: terminate
  handoff_type: condition
  condition_type: expression
  condition_scope: after
  condition: ${is_child_workflow} == True
  transition_target: TerminateTarget
