tools:
- agent: InterviewAgent
  file: interview_seed.py
  function: capture_roast_seed
  description: Capture normalized profile and roast style into context variables.
  tool_type: Agent_Tool
  auto_invoke: false

- agent: HostAgent
  file: roast_stage.py
  function: present_roast_stage
  description: Emit a RoastStageCard artifact summarizing fan-in verdicts.
  tool_type: UI_Tool
  auto_invoke: false
  ui:
    component: RoastStageCard
    mode: artifact

lifecycle_tools: []
