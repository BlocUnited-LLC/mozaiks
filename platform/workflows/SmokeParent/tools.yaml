tools:
  - agent: PresenterAgent
    file: consume_resume_state.py
    function: consume_resume_state
    description: Mark the MFJ resume nonce consumed after presenter output.
    tool_type: Agent_Tool
    auto_invoke: true
    ui:
      component: null
      mode: null

lifecycle_tools: []
